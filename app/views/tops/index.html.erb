<div class="container-fluid">
  <div class="row">
    <%=link_to (tops_path) do %>
      <img src="assets/SDGs.jpeg" class="img-fluid" alt="Responsive image">
    <% end %>

    <% @groups.each do |group|%>
      <%= link_to (group_messages_path(group)) do%>
        <%= image_tag group.image.to_s, class: 'img-fluid' %>
      <% end %>
    <% end %> 

  </div>
</div>
